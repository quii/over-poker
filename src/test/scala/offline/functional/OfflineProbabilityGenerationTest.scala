package offline.functional

import org.scalatest.FunSpec

class OfflineProbabilityGenerationTest extends FunSpec {
  describe("The database entries") {
    it("should contain 52 choose 2 entries - one for each possible player hand") {
      // Pass in the probability function so that it can be stubbed for testing (performance)
    }
  }
}
